import 'package:flutter/material.dart';

class BehaviorChainScreen extends StatelessWidget {
  const BehaviorChainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Behavior Chain Screen'),
      ),
    );
  }
}
