import 'package:flutter/material.dart';

class BehaviorChainScreen extends StatelessWidget {
  const BehaviorChainScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Kedjeanalys')),
      body: const Center(
        child: Text('Behavior Chain Screen - Placeholder'),
      ),
    );
  }
}
